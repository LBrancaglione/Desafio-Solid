using System;

// Foca em salvar os dados do pedido

namespace SrpRightWay
{
    public class RepositorioPedido
    {
        public string Salvar(Pedido pedido)
        {
            // Simula a gravação no banco de dados
            return ("Pedido salvo no banco de dados.");
        }
    }

}